# GLFW (managed drop-in)

Place GLFW headers under `include/GLFW/` and platform libraries in `lib/`
(plus DLLs in `bin/`) if you cannot install the dependency via a package
manager. Files in this tree are ignored by Git except for `.gitkeep`
placeholders.
